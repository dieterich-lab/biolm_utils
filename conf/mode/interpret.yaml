# @package _global_

defaults:
  - data_source: inference
  - inference: default

# Interpret specific settings
task: regression
inference_model:
  pretrainedmodel: null
settings:
  data_pre_processing:
    centertoken: null
  environment:
    ngpus: 1
  training:
    batchsize: 8
    blocksize: 12288
    scaling: log