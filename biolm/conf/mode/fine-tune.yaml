# @package _global_

# Fine-tune specific settings
defaults:
  - /task: ???
  - _self_

mode: fine-tune
data_source:
  crossvalidation: false
  splitratio: [80, 10, 10]
settings:
  mlflow:
    enabled: false
